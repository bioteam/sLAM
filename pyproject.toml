[tool.poetry]
name = "SLAM"
version = "1.0"
description = "Code to create a Small LAnguage Model using personal computing."
authors = ["Brian Osborne <bosborne@alum.mit.edu>"]
readme = "README.md"
packages = [{ include = "SLAM" }]
include = ["src/calculator.py", "src/test.py"]

[tool.poetry.dependencies]
python = "^3.9"
tqdm = "*"
# numpy = "^1.20.0"
# fastapy = "*"
# pandas = "^1.2.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
